version: '3.4'

services:
  gsl.productapi:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:80
      - DB_ENDPOINT=database
      - DB_MIGRATE=true
      - DB_INITIALIZE=true  
    ports:
      - "80"
